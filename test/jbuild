(jbuild_version 1)

(executables
    ((libraries (redis-tls))
     (names (test))
     (modules (test))))

(alias
    ((name runtest)
     (package redis-tls)
     (deps (test.exe))
     (action (run ${<}))))
